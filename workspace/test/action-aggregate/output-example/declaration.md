# Output product 3 — the declaration · submitted at **Step 1 (activation)**

*Submitted with the algorithm for the parent's approval. The node's **manifest / plan / downstream
interface** — what it is, what it will deliver, what it commits to, and what it offers downstream.
Scope figures are the **declared plan** (confirmed at Step 2). First-cut meaning of "declaration" —
to confirm.*

## Identity

- **Node:** the **Action Aggregate** node (a.k.a. action integration node).
- **Fulfilling:** the contract at `../input/contract.md`.
- **Governed by:** the rules `RU-01…RU-11`.

## What I will deliver (plan)

- The **role-action aggregate table** for the D2 product: the actions each role performs, grouped by
  role, stable IDs, each with a **substantial description** and a **Source**.
- **Roles in scope:** the positions of Phase 5 Item 3 plus the **D2 Assistant** — `R-00…R-07`.
- **Action scope:** the **D1 Designer's** involvement (passive + active); the **D2-internal agents'**
  work; and the **downstream product roles'** job functions (D0 Operator, D0/D1 Technical Managers,
  D1 Programmer).

## What I commit to (acceptance)

- **Coverage** — the common, anticipable action set (open-list, not exhaustive).
- **Traceability** — every row Source-cited.
- **Integrity** — stable IDs, deduplicated, grouped by role.
- **Substance** — a substantial description per action and per role.

## Interface I offer downstream

- The completed **aggregate `A-` table** is the **driver/input** for the **capability** node
  (`RU-11`); its sub-contract will be *"design capabilities covering every action in this aggregate."*
- On the parent's approval, this declaration + the aggregate are what the **successor branch**
  (capability → architecture → implementation) consumes to begin.

## Submission plan

- **Step 1 (now):** this declaration + the algorithm, for activation.
- **Step 2 (after the work):** the role-action table with descriptions, as the result.
