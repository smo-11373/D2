# output — the node's produced package (first run)

The Action Aggregate node's re-derived output, generated by running the design-node algorithm over
`../input/` + `../environment/`:

- `role-action-catalog.md` — **Step-2 result**: the role-action table (8 roles `R-00…R-07`, 63 actions
  `A-101…A-163`), grouped by role, Source-cited, each row substantially described. Own `A-1xx` ID
  scheme (independent of the example's historical IDs).
- `algorithm.md` — **Step-1**: the procedure the node ran.
- `declaration.md` — **Step-1**: the node's manifest / downstream interface.

Scored through the harness → `../evaluation/scorecard.md` (**≈ 99/100, "substantially the same"**,
all gates clear). **Read the scorecard's Validity caveat:** engine and judge were the same session,
which had seen `output-example/`, so this is not yet an untainted convergence measurement — a
clean-room run is the next step.
