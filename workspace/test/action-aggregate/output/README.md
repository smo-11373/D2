# output — the node's produced package (clean-room run, run-04, conformant + depth-cue module)

The Action Aggregate node's **untainted** output from the module after **both** improvements —
conformance and the **position-derived depth frame** — synced into `../environment/framework/`. Derived
by a fresh blind agent given only `../environment/` + `../input/`, with no sight of `../output-example/`,
`../work/`, `../evaluation/`, or `../benchmark_verification/`.

- `role-action-catalog.md` — **Step-2 result**: 7 roles (`R-00…R-06`), 47 actions (`A-001…A-048`,
  `A-003` retired/skipped), grouped by role, Source-cited, each row substantially described. The
  operational roles (R-03–R-06) walk the position-derived depth frame (traced in each row's Notes).
- `algorithm.md` — **Step-1**: the procedure the node ran.
- `declaration.md` — **Step-1**: the node's manifest / downstream interface.

**Scored:** `../evaluation/scorecard-run04-depthcue.md` → **conformance gate CLEAR**, composite **≈ 85**.
The depth cue lifted its target — operational-role coverage (R-03–R-06) rose from 10.5 to 18 Σmatch —
but the composite only nudged (84 → 85) because *this* run elaborated the D1 Designer (outside the
frame's scope) less: open-list variance, not the cue's doing. The honest signal is in the targeted
dimension; a single composite is a noisy estimator.

*Baseline (pre-depth-cue, conformant) run-03 archived at `../work/run03-conformant/` with
`../evaluation/scorecard-run03-conformant.md`. Benchmark-validity controls (unaffected by this change)
in `../benchmark_verification/`.*
