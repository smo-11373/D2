# output — EMPTY (for now)

Reserved for what the **future program** produces: the node's own re-derived role-action table,
generated by running the design-node algorithm over `../input/` + `../environment/`.

Intentionally empty until there is an implementation to run.
