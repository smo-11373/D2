# Output product 3 — the declaration (re-derived) · submitted at **Step 1 (activation)**

*The node's manifest / plan / downstream interface — what it is, what it will deliver, what it commits
to, and what it offers downstream. Submitted with the algorithm for the parent's approval.*

## Identity

- **Node:** the **Action Aggregate** node (a.k.a. action integration node).
- **Fulfilling:** the contract at `../input/contract.md`.
- **Governed by:** the rules `RU-01…RU-11`.

## What I will deliver (plan)

- The **role-action aggregate table** for the D2 product: the actions each role performs, grouped by
  role, stable IDs (this run's `A-1xx` scheme), each with a substantial description and a Source.
- **Roles in scope:** the positions of Phase 5 Item 3 plus the **D2 Designer** and the **D2 Assistant**
  — `R-00…R-07`, each tagged intrinsic / default.
- **Action scope:** the **D1 Designer's** involvement (passive + active); the **D2-internal**
  Design Node Builder's work; and the **downstream product roles'** job functions (D1 Programmer,
  D1 Technical Manager, D0 Operator, D0 Technical Manager); plus the D2 Assistant's interaction actions.

## What I commit to (acceptance)

- **Coverage** — the common, anticipable action set (open-list, not exhaustive).
- **Traceability** — every row Source-cited to the frozen inputs.
- **Integrity** — stable, unique IDs, deduplicated, grouped by role.
- **Substance** — a substantial description per action and per role.

## Interface I offer downstream

- The completed **aggregate `A-` table** is the **driver/input** for the **Capability** node
  (`RU-11`); its sub-contract will be *"design the capabilities that support every action in this
  aggregate"* — coverage is the contract.
- On the parent's approval, this declaration + the aggregate are what the **successor branch**
  (capability → architecture → implementation) consumes to begin.

## Submission plan

- **Step 1 (now):** this declaration + the algorithm, for activation.
- **Step 2 (after the work):** the role-action table with descriptions, as the result.

## Planned scope figures (confirmed at Step 2)

- **8 roles** (`R-00…R-07`); **63 actions** (`A-101…A-163`), grouped 3 / 24 / 8 / 3 / 7 / 8 / 7 / 3.
