# environment — fundamentals + module + inherited aggregate (what the Experience node reads)

Read-only material the Experience node reads. **Self-contained.**

- `constitution/` — the frozen **Phases 1–5** + `completions.md` (Designer-originated completions that govern
  alongside the baseline; `C-2026-07-19-1` names the **D2 Assistant**, the interaction surface UX designs).
- `method.md`, `rules.md`, `glossary.md`, `sources.md` — the method (§1), rules (`RU-*`), terms, and the
  Source namespace.
- `framework/` — the **module** (design tree + design node module) — the same method the action-aggregate
  node ran; the Experience node runs it too (self-similar module).
- `action-aggregate/` — the **inherited, accepted action aggregate** (parent-owned, read-only) the UX layout
  must cover. See its `README.md`.

*Snapshot of the D2 fundamentals + module (same as the action-aggregate test) plus the inherited aggregate.*
