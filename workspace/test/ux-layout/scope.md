# Scope & deliberately-open questions — the Experience-node test

## In focus now

- The **Experience node's derivation**: given the accepted **action aggregate**, produce a **UX layout** —
  the interaction pattern each action maps to, per position, attention-budgeted.
- The **exemplar-anchored benchmark**: a **positive + negative UX pair** (no derivable answer key), scored
  by proximity to the poles (`evaluation/`, `benchmark_verification/`).
- **Iterative bar-raising**: the positive is **v0 (deliberately modest)**; raise it, versioned, as we learn.

## Exemplar-anchored, not derivable-target

There is **no `output-example`**. "Good UX" is not derivable from the fundamentals the way the action set
was; it is **pointed at** (positive) and **contrasted** (negative), with every rubric dimension anchored to
a fundamental so the bar rises toward the constitution, not taste. Scoring is **position between poles**,
not recall of a fixed set.

## Deliberately not addressed yet (open)

1. **The UX layout's output format is fluid.** v0 pins only the *substance* (patterns × actions × attention),
   not a fixed schema. Layer-A structural checking waits until the format is pinned (`evaluation/scoring-method.md`).
2. **Spawning / submission machinery — defined, not exercised.** This test isolates the *derivation*. The
   **Action → Experience** cascade — *when* the parent spawns the child (after Step-2 acceptance of the
   aggregate, `RU-11`), the **contract instantiated from the standard template**, and the **two-step
   submit/accept** (`RU-02`) — is defined in the fundamentals/module and `input/contract.md`, but **not run**
   here. A later test (v2) exercises the machinery end-to-end.
3. **The contract template.** `input/contract.md` is an **instantiation** of the same standard template the
   action-aggregate contract used (Q1: fill-the-template, not freehand). Extracting that template as an
   explicit module asset is a pending consolidation.
4. **Multi-layer UX.** v0 anchors on the **D1 Designer's** experience of D2. The **D0-operator** experience
   (the D0-user throughline) and the D1 technical-manager experience are in-scope for the node but not yet in
   the v0 pair — candidates for a bar-raise.

## Status of the pair

**v0 — deliberately modest.** Three canonical patterns (Review Stop, Monitoring, Clarification). Expect it
to be **raised** (more patterns, tighter attention-budget, added dimensions) — each raise a recorded version.
