# context — governing inputs (not the focus)

Read-only material the action aggregate and its production branch operate under (`RU-08`). These are
the **fundamentals** (method + rules); they govern, they are not produced by this run.

- `method.md` — the **Phase 6 method** (functional doc §1).
- `rules.md` — the **design-tree rules** `RU-01…RU-11`.

*(The **role table** is **not** here — it is produced inside the production branch, at
`../action-aggregate/roles/`, because "seek out the roles" is the first production step.)*
