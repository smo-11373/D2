# context — inputs to the action aggregate (not the focus)

Read-only material the **action-aggregate** node references (`RU-08`). These are *inputs*; the action
aggregate is the top of this run.

- `role-table.md` — the **role table** (`R-`): the roles the actions serve. Authored by the Role node
  (out of focus here); supplied read-only.
- `method.md` — the **Phase 6 method** (functional doc §1).
- `rules.md` — the **design-tree rules** `RU-01…RU-11`.

The role table + method + rules govern and feed the action aggregate; to change the role table, the
action aggregate would **propose up** to the Role node (`RU-04`, `RU-09`) — not shown in this focused
run.
