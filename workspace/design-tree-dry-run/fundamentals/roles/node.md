# Node: Roles

- **Authors / owns:** `role-table.md` — the **role table `R-`** (R-00…R-07, each tagged
  intrinsic / default). Related action `A-056`; capability `C-38`.
- **Contract received (from Fundamentals):** the method + rules + link to Phases 1–5.
- **Contract to child (Role-Action):** *"provide the **role-action table as an aggregate**"* (`RU-10`),
  plus the **role table as read-only input** (`RU-08`). The child may not author the role table; to
  change it, the child **proposes up** and Roles decides (`RU-04`, `RU-09`).
- **Justification (`RU-02`):** Human Position First (Phase 5 §Item 3); the intrinsic/default
  provenance (functional doc §3a).
- **Children:** `role-action/`.
