# Node: Fundamentals (root)

- **Authors / owns:**
  - `method.md` — the Phase 6 method (functional doc §1).
  - `rules.md` — the design-tree rules `RU-01…RU-10`.
  - *(references)* the D2 constitution — Phases 1–5, in `../../../ref/` — read, not re-authored.
- **Contract received:** none (root; the constitution is its given ground).
- **Contract to children:** passes down the method + rules + a link to Phases 1–5. Demands each
  child deliver its own aggregate (`RU-10`).
- **Justification (`RU-02`):** §1's argument, grounded in Phases 1–5. Attached here for the
  Designer's confirmation.
- **Children:** `roles/`.
- **Deferred:** Fundamentals is itself a five-phase body of work and would decompose into its own
  subtree on the same rules — the harder case, not expanded in this run.
