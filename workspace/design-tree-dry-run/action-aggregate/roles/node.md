# Node: Roles (production branch — step 1)

*Seek out the roles **first**; the per-role action work depends on this.*

- **Authors / owns:** `role-table.md` — the **role table `R-`** (R-00…R-07, each tagged
  intrinsic / default). Related action `A-056`; capability `C-38`.
- **Contract received (from the action aggregate):** establish the roles the design recognizes.
- **Contract to child (`actions-by-role/`):** the **role table**, read-only, as the basis for
  per-role actions (`RU-08`). To change it later, a descendant **proposes up** (`RU-04`, `RU-09`).
- **Justification (`RU-02`):** Human Position First (Phase 5 §Item 3); intrinsic/default provenance.
