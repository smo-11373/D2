# D2 / workspace

The **design workspace** for D2 — intermediate, in-process material.

- **Owned by:** the D2 layer.
- Holds: scratch, temp, experiments, derived evidence, and the **verification harness** over
  test D1 projects (e.g. `workspace/verification/` referencing sibling `D1-*` roots).
- Nothing here is authoritative; promote settled results into `../design/`.
