# design / phase-6 — Role–Action & Capability Model (active)

The messy, in-progress design work for **Phase 6**. Governing hierarchy:
Role/Position → Action → Capability → Architecture → Implementation (architecture deferred to
Phase 7). Phase 6 establishes *what D2 must be capable of*, not how it is built.

**The Phase 6 design document (substance): [`phase-6-role-action-and-capability-model.md`](phase-6-role-action-and-capability-model.md).**
The catalogs in `../catalogs/` (role-action, capabilities, action-capability-map, designer-queries,
rules) are the **bookkeeping registries** for that model — stable IDs, foreign keys, status — not the
substance. Also here: `prior-items/` (superseded formulation), `audit-1-…` (coverage audit),
`working-notes.md`, and `phase-6-completion-summary.md`.
